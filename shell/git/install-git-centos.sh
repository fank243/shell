# 卸载
sudo yum remove git

# 安装软件
sudo yum install git

git --version

# 配置全局变量
git config --global user.email "fank243@163.com"
git config --global user.name "FanWeiJie"

git config --list

echo "git install finished"
